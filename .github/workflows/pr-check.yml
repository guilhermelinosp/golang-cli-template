name: pr-check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  checks: write
  security-events: write

concurrency:
  group: pr-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    uses: guilhermelinosp/templates/.github/workflows/shellcheck.yml@9fe39322263723d320cdce780f00d5926059798a # main

  merge-check:
    uses: guilhermelinosp/templates/.github/workflows/merge-check.yml@9fe39322263723d320cdce780f00d5926059798a # main

  gitleaks:
    uses: guilhermelinosp/templates/.github/workflows/gitleaks.yml@9fe39322263723d320cdce780f00d5926059798a # main

  labeler:
    uses: guilhermelinosp/templates/.github/workflows/labeler.yml@9fe39322263723d320cdce780f00d5926059798a # main

  # Full Go gate from the org reusable: module integrity, vet, boundary
  # guard, race tests + coverage, canonical lint, build smoke and
  # dependency review (embedded).
  go-quality:
    uses: guilhermelinosp/templates/.github/workflows/go-quality.yml@9fe39322263723d320cdce780f00d5926059798a # main
    with:
      extra-commands: make verify
      build-command: make build
      smoke-command: ./bin/golang-cli-template --version

  govulncheck:
    uses: guilhermelinosp/templates/.github/workflows/govulncheck.yml@9fe39322263723d320cdce780f00d5926059798a # main

# CodeQL runs via the per-repo inline workflow (.github/workflows/codeql.yml):
# the reusable variant still triggers GitHub-side startup_failure across
# callers regardless of content/pinning; revisit after a clean-room repro.
